version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No dependencies to install - static site"
    build:
      commands:
        - echo "No build step required - static HTML/CSS/JS site"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
